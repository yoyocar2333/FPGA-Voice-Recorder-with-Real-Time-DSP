name: rtl-smoke
on: [push, pull_request]
jobs:
  compile-dsp:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y iverilog
      - run: iverilog -g2012 -s AudDSP -o /tmp/auddsp src/AudDSP.sv
